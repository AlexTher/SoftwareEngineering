<!DOCTYPE html>
<html>
    <head>
        <!--Header-->
	    <%- include('../partials/teacher_header.ejs')%>

        <link rel="stylesheet" href="/css/style.css">
        
    </head>

    <body>
        <h1>Classes</h1>

        <div class="container">
            <div class="row" id="waitlistEntry">
              <% if (wishlist.length > 0) { %>
                <%- include('../partials/classEntries/teacherClassEntry', { wishlist: wishlist }) %>
              <% } else { %>
                <p>You don't have any classes in your waitlist.</p>
              <% } %>
            </div>
        </div>
    </body>
</html>